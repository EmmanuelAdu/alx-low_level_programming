#include <stdio.h>

/**
 * main - this function prints numbers from 0 to 9
 *
 * Return: always when done
 */

int main(void)
{
	int e;

	for (e = 0 ; e <= 9 ; e++)
	putchar(e);
putchar('\n');
return (0);
}

