#include <stdio.h>

/**
 * main - this prints all alphabets in lowercase
 *
 * Return: when successful
 *
 */

int main(void)

{
	char i;

	for (i = 'a' ; i <= 'z' ; i++)
	putchar (i);
	putchar ('\n');
	return (0);

}
